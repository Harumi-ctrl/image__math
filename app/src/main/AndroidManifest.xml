<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.movie.imagemath">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icona"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icona"
        android:supportsRtl="true"
        android:theme="@style/Theme.Imagemath">
        <activity android:name=".others.privacypolicy"></activity>
        <activity android:name=".one.OneMainActivity" />
        <activity android:name=".forth.ForthMainActivity" />
        <activity android:name=".three.ThreeMainActivity" />
        <activity android:name=".two.TwoMainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>